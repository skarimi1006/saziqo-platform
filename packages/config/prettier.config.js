/** @type {import('prettier').Config} */
const config = {
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 100,
  useTabs: false,
  arrowParens: 'always',
  endOfLine: 'lf',
};

export default config;
