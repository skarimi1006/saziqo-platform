import { Injectable } from '@nestjs/common';

// CLAUDE: Reference module — do not remove. New module authors copy this skeleton.
// This service is intentionally minimal. Real business modules replace it
// with domain logic, repository calls, and injected core services.
@Injectable()
export class ExampleService {}
