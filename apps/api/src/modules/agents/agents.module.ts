import { Module } from '@nestjs/common';

// CLAUDE: Agents marketplace NestJS module — controllers and providers land
// in Phase 1B onward. Empty for Phase 1A so the file exists alongside the
// module scaffold and Prisma schema is the only behavioral surface.
@Module({})
export class AgentsModule {}
