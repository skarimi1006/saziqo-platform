-- Placeholder for an orphaned migration recorded in _prisma_migrations
-- but whose original file was deleted when it was renamed (with a final
-- "s") to 20260501000000_add_impersonation_sessions. Prisma compares
-- migrations by directory name; this file lets the local migrations
-- folder match the historical state recorded in the dev database
-- without re-running the schema changes (which the renamed migration
-- already produced).
SELECT 1;
